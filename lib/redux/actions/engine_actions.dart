/// Actions used by the game/engine without user input

/// Loads required gameplay assets from disk
class LoadAssetsAction {}

/// Notifies the system of a new frame tick for automation
class NextFrameAction {
  NextFrameAction();
}